# 快速开始

## 全局安装脚手架

```ts
npm install my-vue-vite-cli -g
```

## 初始化项目

```ts
my-vue init <programName>
```

## 初始化成功将会自动安装依赖， 依赖安装之后，将会直接运行

- 到这里， 框架的基础包就安装完成； 这个基础包是初始化即可正常使用 vue 的基础功能 —— 功能包含但不限于：

  路由的跳转配置+导航守卫的处理、状态管理、国际化、基础组件的使用使用等， 相关依赖这里都已经做了自动安装
